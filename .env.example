# Cloudflare R2 配置示例
# 请复制此文件为 .env 并填入真实的配置值

# Cloudflare 账户 ID
# 可在 Cloudflare Dashboard 右侧边栏找到
R2_ACCOUNT_ID=your-account-id-here

# R2 API 访问密钥 ID
# 在 Cloudflare Dashboard > R2 > 管理 R2 API 令牌 中创建
R2_ACCESS_KEY_ID=your-access-key-id-here

# R2 API 秘密访问密钥
# 创建 API 令牌时生成，请妥善保管
R2_SECRET_ACCESS_KEY=your-secret-access-key-here

# R2 存储桶名称
# 在 Cloudflare Dashboard > R2 中创建的存储桶名称
R2_BUCKET_NAME=your-bucket-name-here

# R2 区域（可选，通常使用 "auto"）
R2_REGION=auto

# 自定义域名（可选）
# 如果为 R2 存储桶配置了自定义域名，请在此填入
# 例如: feeds.yourdomain.com
R2_CUSTOM_DOMAIN=your-custom-domain.com

# 其他可选配置
# 可根据需要添加其他环境变量